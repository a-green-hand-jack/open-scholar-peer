{
  "name": "open-scholar-peer",
  "version": "2.1.0",
  "description": "OpenCode-native standalone Paper Review Agent",
  "type": "module",
  "bin": {
    "osp": "./dist/cli.js",
    "open-scholar-peer": "./dist/cli.js"
  },
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "prepare": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@opencode-ai/sdk": "^1.18.11",
    "commander": "^12.1.0",
    "execa": "^9.5.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  }
}
